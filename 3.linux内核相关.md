1. ENTRY 宏是什么意思？
在 Linux 内核的汇编代码中，ENTRY 和 ENDPROC 是一对宏，用于定义全局可见的函数。

定义位置
c
// 在 include/linux/linkage.h 中定义
#define ENTRY(name) \
    .globl name;    \
    .align 4;       \
    name:
展开后的实际代码
你看到的：

assembly
ENTRY(__asm_copy_to_user)
ENTRY(__asm_copy_from_user)
展开后实际上是：

assembly
.globl __asm_copy_to_user    # 声明为全局符号，可被其他文件引用
.align 4                      # 4字节对齐
__asm_copy_to_user:           # 函数入口标签
.globl __asm_copy_from_user
.align 4
__asm_copy_from_user:         # 函数入口标签
ENTRY vs EXPORT_SYMBOL 的区别
宏	作用	可见范围
ENTRY	定义函数的入口点（标签），让汇编器知道这是一个函数	整个内核（链接器层面）
EXPORT_SYMBOL	将符号导出到内核符号表，供模块使用	内核模块（运行时动态链接）
两者必须配合使用：

ENTRY 让链接器知道这个符号存在

EXPORT_SYMBOL 让模块加载器知道可以调用这个函数
