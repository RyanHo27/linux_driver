# 虚拟内存
**在linux用户态的虚拟内存中，.text段并不是从0x0000 0000开始的，在这之前还有一段128M的空白，为什么要有这个?**

1. 拦截“空指针异常”（Null Pointer Dereference）这是最核心的原因。

什么是空指针？ 

在 C/C++ 中，NULL 指针的本质就是 (void*)0。

为什么留出空白？ 

如果 .text 段或者数据段从 0x0 开始，那么一旦程序中出现逻辑错误（例如未初始化指针、误用空指针），程序尝试去读写 0x0 地址时，就可能会误读或误写代码段的内容。保护机制：内核故意将从 0x0 开始的这片区域（默认至少 64KB，现代系统通常更大）设置为不可读、不可写、不可执行（没有任何页表映射）。这样，任何对这片区域的访问都会瞬间触发 CPU 的硬件保护，直接抛出 Segmentation fault（段错误）并让程序崩溃。这能帮助程序员在第一时间精准定位空指针死穴，防止数据被悄悄污染。
