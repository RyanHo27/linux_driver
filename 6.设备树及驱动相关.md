关于 平台设备结构体中resource

---

| 设备树属性（DTS） | 为什么不能进 resource？ | 驱动在 probe 时怎么拿到它？ |
| :--- | :--- | :--- |
| **`compatible = "ns16550a";`** | 它是用来匹配驱动的字符串，不是硬件资源。 | 无需手动拿，内核匹配成功后存在 `pdev->dev.of_node` 里。 |
| **`status = "okay";`** | 它是开关状态，不是资源。 | 内核在解析时如果看到不是 `"okay"`，根本就不会为它创建 `platform_device`。 |
| **`clocks = <&clk_sys>;`** | 时钟是一个复杂的树状拓扑对象，无法用一个 `start` 和 `end` 的数值来表达。 | 驱动通过 `devm_clk_get(dev, NULL)` 专属 API 去时钟子系统里抓取。 |
| **`gpios = <&gpioa 5 0>;`** | GPIO 引脚虽然有编号，但它带有复杂的控制逻辑和引脚复用，由专用的 GPIO 子系统管理。 | 驱动通过 `devm_gpiod_get(dev, "gpios", ...)` 专属 API 获取。 |
| **`clock-frequency = <40000000>;`** | 这是一个纯数字配置参数（40MHz 频率），不是硬件物理实体。 | 驱动通过 `of_property_read_u32(node, "clock-frequency", &val)` 读取。 |
