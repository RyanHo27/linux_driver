# 驱动进程
## 进程占用驱动导致无法卸载情况
查找对应的PID：

1. lsof /dev/MyCharDev0
2. fuser /dev/MyCharDev0

## 进程fd值
进入c程序后，进行open操作，此时返回的fd值为3.

为什么为3？有什么讲究吗？
在 Linux 系统中，新打开的文件返回 fd = 3 并不是随机的，而是由以下两个核心机制决定的：
1. 标准输入、输出、错误占用了 0, 1, 2。在 Linux 中，当一个新进程（你的用户程序）被创建并初始化时，内核会默认自动为它打开三个文件流，并固定占用前三个文件描述符（File Descriptor，简称 fd）

0 (STDIN_FILENO)：标准输入（通常对应键盘输入）1 (STDOUT_FILENO)：标准输出（通常对应终端屏幕输出）2 (STDERR_FILENO)：标准错误（通常对应终端屏幕输出，用于打印报错信息）

因为 0、1、2 已经被这三个默认的文件流死死占住了，所以任何进程在启动后，主动打开的第一个新文件（或设备），其 fd 必然是从 3 开始分配。

2. Linux 的“最小未用分配原则”Linux 内核在为进程分配文件描述符时，遵循一个严格的算法规则：每次打开新文件时，总是返回当前进程中“数值最小且尚未被使用”的那个 fd。

## 驱动中延时
**忙等**
如果延时时间极短（纳秒/微秒级），或者代码当前处于中断上下文、持有锁的状态，无法进行休眠，则必须使用忙等待。它通过让 CPU 执行空循环来实现精确延时。
1. ndelay (纳秒级)
   1. 用途：极短的纳秒级延时。
   2. 用法：ndelay(10); // 延时 10ns
2. udelay (微秒级)
   1. 用途：微秒级（通常小于 10 微秒）的短延时。
   2. 用法：udelay(5); // 延时 5μs
   3. 特点：在硬件初始化或读写特定寄存器（如 I2C/SPI 时序模拟）时非常常用。
3. mdelay (毫秒级，不推荐)用途：毫秒级忙等待。
   1. 用法：mdelay(2); // 延时 2ms
   2. 特点：它会让 CPU 傻傻地死等几毫秒，严重浪费内核资源并影响系统响应。除非在中断里实在没办法，否则强烈不推荐使用，请用 msleep 代替。



**休眠**
如果延时时间较长（通常大于 10 微秒），且当前函数运行在进程上下文（可以被调度），应当优先选择休眠延时。这类函数会释放 CPU，让其他任务运行。
1. msleep (毫秒级)
   1. 延时 10 毫秒（ms） 以上。
   2. 用法：msleep(20); // 延时 20ms
   3. 特点：使当前进程进入不可中断的休眠状态，直到时间到达。
2. usleep_range (微秒级)
   1. 延时 10 微秒（μs）到 20 毫秒（ms） 之间。
   2. 用法：usleep_range(min, max);例如：usleep_range(10, 50); // 告诉内核延时 10 到 50 微秒之间即可。
   3. 特点：非常高效。它允许内核将多个定时器合并触发，从而减少 CPU 唤醒次数，节省功耗。
3. ssleep (秒级)用途：延时 1 秒 以上。
   1. 用法：ssleep(2); // 延时 2 秒。

## kmalloc()和vmalloc(）
**kmalloc**
用于申请较小的、连续的物理内存
1. 以字节为单位进行分配，在<linux/slab.h>中
2. void *kmalloc(size_t size, int flags) 分配的内存物理地址上连续，虚拟地址上自然连续
3. gfp_mask标志：什么时候使用哪种标志？
   1. 进程上下文，可以睡眠 GFP_KERNEL
   2. 进程上下文，不可以睡眠 GFP_ATOMIC
   3. 中断处理程序 GFP_ATOMIC
   4. 软中断 GFP_ATOMIC
   5. Tasklet GFP_ATOMIC
   6. 用于DMA的内存，可以睡眠 GFP_DMA | GFP_KERNEL
   7. 用于DMA的内存，不可以睡眠 GFP_DMA | GFP_ATOMIC
4. void kfree(const void *ptr) 释放由kmalloc()分配出来的内存块

---

**vmalloc**
用于申请较大的内存空间，虚拟内存是连续的
1. 以字节为单位进行分配，在<linux/vmalloc.h>中
2. void *vmalloc(unsigned long size) 分配的内存虚拟地址上连续，物理地址不连续
3. 一般情况下，只有硬件设备才需要物理地址连续的内存，因为硬件设备往往存在于MMU之外，根本不了解虚拟地址；但为了性能上的考虑，内核中一般使用 kmalloc()，而只有在需要获得大块内存时才使用vmalloc()，例如当模块被动态加载到内核当中时，就把模块装载到由vmalloc()分配 的内存上。
4.void vfree(void *addr)，这个函数可以睡眠，因此不能从中断上下文调用。

---

**malloc(), vmalloc()和kmalloc()区别**
1. kmalloc和vmalloc是分配的是内核的内存,malloc分配的是用户的内存（内核内存和用户内存均为虚拟内存）
2. kmalloc保证分配的内存在物理上是连续的,vmalloc保证的是在虚拟地址空间上的连续,malloc只在虚拟地址空间上连续
3. kmalloc能分配的大小有限,vmalloc和malloc能分配的大小相对较大
4. vmalloc比kmalloc要慢


**kmalloc()与vmalloc()的区别**
这两个函数所分配的内存都处于内核空间，即从3GB～4GB；但位置不同，kmalloc()分配的内存处于3GB～high_memory之间，而vmalloc()分配的内存在VMALLOC_START～4GB之间，也就是非连续内存区。一般情况下在驱动程序中都是调用kmalloc()来给数据结构分配内存，而vmalloc()用在为活动的交换区分配数据结构，为某些I/O驱动程序分配缓冲区，或为模块分配空间，例如在include/asm-i386/module.h中定义了如下语句：

  #define module_map(x)           vmalloc(x)

  其含义就是把模块映射到非连续的内存区。

 与kmalloc()和vmalloc()相对应，两个释放内存的函数为kfree()和vfree()。

---

**多进程情况，一个进程fork另一个进程，父进程退出后，打开的fd、pid是如何继承的？**

**fd**

当父进程fork自进程时，在内核层面，子进程会把这个文件描述符表（struct fdtable）中的指针数组（struct file **fd）完整地复制一份，但它复制的是指针本身（浅拷贝），而不是指针指向的 struct file 实例。

**fork() 继承时的内核行为（到底复制了什么？**

当父进程调用 fork() 时，内核会调用 dup_fd() 函数来处理文件描述符。它的行为就像是在做浅拷贝（Shallow Copy）：
1. 新建一个数组： 内核为子进程创建一个全新的 struct files_struct 和一个新的指针数组（FD 表）。
2. 复制指针值： 内核把父进程数组里保存的那些 struct file * 指针，原封不动地复制到子进程的新数组中。
3. 增加引用计数： 因为子进程的数组也指向了相同的 struct file 实例，内核会将这些 struct file 的引用计数（f_count）加 1。

   📌 视觉对比：
   1. fork 前：父进程 fd[3]  ➡️ struct file (A) [引用计数 = 1]
   2. fork 后：父进程 fd[3]  ➡️ struct file (A) [引用计数 = 2] ⬅️ 子进程 fd[3]

**2 个关键技术特性**

特性一：共享文件偏移量（Offset）

因为父子进程的 FD 指向同一个 struct file 实例，而文件的读写位置（f_pos）是存在 struct file 里面的。

现象： 如果父进程读取了文件的前 10 个字节，文件的偏移量变成了 10。此时子进程接着去读，会从第 11 个字节开始读，而不是从头开始。它们在共同推进同一个进度条。


特性二：解耦的关闭操作（close）

因为有引用计数保护。

现象： 就像前面提到的，父进程退出或者手动调用 close(3)，内核只会把 struct file (A) 的引用计数减 1（从 2 变成 1），并释放父进程自己的指针。由于子进程还在指向它（计数为 1），这个文件实例在内核中依然存活，子进程完全可以正常读写。只有当父子进程都 close 了这个 FD，文件才会被真正关闭。


**struct file**

在 Linux 内核中，struct file 存储在内核的 SLAB/SLUB 分配器所管理的专属内存池中
1. 专属内存池： 内核在启动时，会通过 kmem_cache_create() 创建一个名为 "files_cache" 的专属高速缓存池。
2. 动态分配： 每当任何一个进程调用 open() 系统调用时，内核就会通过 kmem_cache_alloc() 从这个 "files_cache" 池子中切出一块固定大小的内存，用来存放这个新创建的 struct file 实例。
3. 安全性： 因为它在内核空间，用户进程是绝对无法直接访问或修改这块内存的。进程只能拿到一个整数（FD 号）传给内核，由内核去查表、找指针、操作 struct file。

进程通过fd读写文件调用链

[ 用户态 (User Space) ]

  进程 A 的代码 ──> 调用 read(3, buf, 10) 
  
──────系统调用 (ecall 汇编指令) 跨越边界 ──────

[ 内核态 (Kernel Space) ]

  1. 进程 A 的 task_struct ──> 找到其中的 fd_array[3]
  2. 顺着 fd_array[3] 这个【钩子】 ──> 定位到内核 files_cache 中的【struct file】
  3. 检查 struct file 的权限，读取里面的 f_pos（当前偏移量）
  4. 通过 struct file 里的 f_op 指针 ──> 调用具体文件系统的读函数（如 ext4_file_read）
  5. 通过 struct file 里的 f_inode 指针 ──> 找到文件的 VFS 索引节点（struct inode，代表硬盘上的真实文件）
  6. 从驱动和硬盘读取数据，更新 struct file 中的 f_pos，返回用户态

---

**pid**

 PID 在 fork() 瞬间的“继承”与分配
 1. 当父进程调用 fork()，子进程会获得一个在当前 PID 命名空间（Namespace）内全新的、唯一的正整数作为自己的 PID。
 2. 32 位系统默认的 PID 最大值（pid_max）通常是 32768（可以通过 /proc/sys/kernel/pid_max 查看）。内核通过一个位图（Bitmap）来管理，一旦分配到最大值，就会回绕到低位重新寻找未使用的数字。
 3. 子进程虽然不继承父进程的 PID 值，但它会在自己的进程控制块（struct task_struct）中，牢牢记住父进程的 PID。
    1. 内核会把父进程的 PID 赋值给子进程的 tgid（线程组 ID，如果是单线程进程，tgid 就等于 pid）以及 parent 指针。
    2. 子进程在任何时候调用 getppid()，内核都会去读取这个记录，返回父进程的 PID。



父进程退出后，PID 关系变化
1. 父进程退出时，内核会调用 do_exit()，子进程变为“孤儿进程”（Orphan Process）。由于一个进程不能没有“父亲”（否则死后无人收尸），内核会执行一个叫做 Reparent（重新寻父） 的操作。
2. 孤儿进程会被系统的 init 进程（PID 1） 强制收养。此时：子进程的真实 PID 保持不变。子进程再次调用 getppid() 时，返回的值会从原来的父进程 PID 突变为 1。
3. 只要子进程退出，就会变成僵尸进程（留有退出码和其pid号等待父进程确定）。当父进程wait之后，内核就会释放僵尸进程占用的pid号，至此。子进程所有资源全部释放（exit就已经释放了大部分的资源，只保留了退出码和pid）。


---

## 惊群”现象（Thundering Herd）及解决

主要发生在网络编程和用户态高并发应用中，当大量进程/线程在等同一个事件（如：新连接到来、文件描述符可读），事件一旦发生，操作系统同时唤醒所有等待者，但最终只有一个进程能处理它，其余进程白白浪费 CPU。


---




























